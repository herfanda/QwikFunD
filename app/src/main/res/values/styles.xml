<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/bg_main_activity</item>
        <item name="colorPrimaryDark">@color/bg_secondary_activity</item>
        <item name="colorAccent">@color/bg_third_activity</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/holo_orange_light</item>
    </style>

    <style name="loginThemeEditText" parent="@android:style/Widget.EditText">
        <item name="colorControlNormal">@color/layout_white</item>
        <item name="android:textColorHint">@color/layout_white</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
